import {StrictMode} from 'react';
import {creatRoot} from 'react-dom/client';
import App from './App.tsx';
import './index.css';

creactRoot(document.getElementById(root)!).render(
    <StrictMode>
    <App/>
    </StrictMode,
);